import 'package:flutter/material.dart';

/// على الموبايل: نستخدم video_player دائماً (الرابط المحوّل)
Widget buildVideoBackground({
  required String url,
  required Widget fallback,
}) {
  return fallback;
}
